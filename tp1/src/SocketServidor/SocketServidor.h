#ifndef SOCKETSERVIDOR_H_
#define SOCKETSERVIDOR_H_

/*
class SocketServidor {

/*
private:
	SocketServidor(int fd);
	SocketServidor(const Socket& original);
	int fd;
	bool valido;
	std::string ip;


public:
	SocketServidor();
	virtual ~SocketServidor();
	int recibirExacto(char* stream, unsigned int size);
	int conectar(const std::string &host,unsigned int port);
	int escuchar(unsigned int port,unsigned int numClientesEspera);
	int cerrar();
	int enviar(char* stream,unsigned int size);
	int recibir(char* stream,unsigned int size);
	Socket* aceptar();
	bool es_valido();
	std::string getIp();
	void setIp(int ip);
};
*/
#endif /* SOCKETSERVIDOR_H_ */
